workflows:
  ios-workflow:
    name: iOS Build
    max_build_duration: 120
    environment:
      groups:
        - codemagic_secrets
      vars:
        APP_ID: "com.beatdz.langlalau"
    scripts:
      - name: Use Gradle 7.6.4
        script: |
          sed -i '' 's/gradle-8.7-bin.zip/gradle-7.6.4-bin.zip/g' gradle/wrapper/gradle-wrapper.properties
      - name: List ALL tasks (with plugins)
        script: |
          chmod +x gradlew
          ./gradlew :ios:tasks --all 2>&1
      - name: Build iOS app
        script: |
          chmod +x gradlew
          ./gradlew :ios:assemble :ios:jar 2>&1 | tail -50
